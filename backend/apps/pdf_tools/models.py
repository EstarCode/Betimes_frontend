"""
Models for PDF tools (merge, split, rotate, watermark).
"""

from django.db import models

# PDF tools will use the same job tracking pattern as conversion
# For brevity, using a simplified model structure
